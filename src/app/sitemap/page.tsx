/**
 * Visual Sitemap deactivated to avoid conflict with sitemap.xml metadata generator.
 * All visual sitemap functionality has been moved to src/app/site-map/page.tsx
 * This file is kept empty to prevent Next.js from creating a route conflict.
 */
export default function SitemapConflictResolver() {
  return null;
}
